name: Fall AI
version: "0.1.3"
slug: fall_ai
description: CPU-efficient YOLO11n pose fall detection for Home Assistant
arch:
  - amd64
startup: application
boot: auto
init: false
map:
  - addon_config:rw
options:
  log_level: info
schema:
  log_level: list(debug|info|warning|error)
